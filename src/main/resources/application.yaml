spring:
  application:
    name: SEOgenix

  data:
    mongodb:
      uri: ${MONGO_URI}
    redis:
      host: ${REDIS_URL}
      port: ${REDIS_PORT}
      connect-timeout: 60000

  openai:
    key: ${OPEN_ROUTER_KEY}
  web:
    resources:
      add-mappings: false